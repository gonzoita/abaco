# Variables de entorno (Contiene API keys y claves DB)
.env
.env.local
.env.production

# Dependencias de Node
node_modules/
frontend/node_modules/

# Dependencias de Composer (solo se usan para correr pruebas en CI, no se
# despliegan a producción)
vendor/
.phpunit.result.cache

# Archivos del sistema y temporales
.DS_Store
Thumbs.db
*.log
.gemini/
backend/logs/
graphify-out/

# IDEs
.vscode/
.idea/
